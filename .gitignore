node_modules/
.turbo/
dist/
.next/
out/
artifacts/
cache/
typechain-types/
coverage/
.env
.env.local
*.log
.DS_Store
.pnpm-store/

# Deployment addresses are per-machine and regenerated by deploy:local
deployments/

# TypeScript / Next generated
*.tsbuildinfo
next-env.d.ts

# Playwright's saved sign-in state holds a live session token - never commit it.
# Patterns with a slash anchor to this file's directory, so these need the
# leading ** to match apps/web/playwright/.auth/.
**/playwright/.auth/
**/test-results/
**/playwright-report/
