{
  "name": "@sih26125/identity",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "did-jwt": "^8.0.18",
    "did-jwt-vc": "^4.0.16",
    "ethr-did-resolver": "^11.0.0",
    "did-resolver": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
